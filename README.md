# Dicee 🎲

A simple 2-player dice game built using HTML, CSS, and JavaScript. The dice images update dynamically on page refresh to show which player wins.

## 🚀 How It Works
- Each page refresh generates two random dice numbers (1–6)
- Dice images update dynamically based on the random numbers
- The winner is determined by comparing the two numbers

## 🛠 Built With
- HTML
- CSS
- JavaScript
- Google Fonts: Indie Flower & Lobster

## 📚 What I Learned
- Math.random() for generating random numbers
- Changing image sources dynamically with JavaScript (`.src`)
- Manipulating DOM elements (`querySelector`, `.innerHTML`)
- Styling and structuring a small web game

## 💻 How to Run
1. **Clone the repository**  
- git clone https://github.com/your-username/dicee-game-js.git

2. **Open the project folder**

3. **Double-click index.html**

- The game will open in your default browser

4. **Or open via a code editor live server**

- If you use VS Code, install the Live Server extension

- Right-click index.html → Open with Live Server

⚡ No backend needed—pure front-end project

💡 Future Improvements

- Add a "Roll Dice" button instead of relying on page refresh

- Keep track of score over multiple rounds

- Add animations or sound effects
